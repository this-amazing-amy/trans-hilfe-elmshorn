---
export interface Props {
  title: string;
}

const { title } = Astro.props;

import Navigation from '../components/navigation.astro'

const now = new Date();
const currentYear = now.getFullYear();
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width"/>
    <link rel="icon" type="image/svg+xml" href="/logo.jpg"/>
    <meta name="generator" content={Astro.generator}/>
    <title>Trans Hilfe Elmshorn - {title}</title>
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-4GRJLSSNZQ"></script>
    <script>
      window.dataLayer = window.dataLayer || [];

      function gtag() {
        dataLayer.push(arguments);
      }

      gtag('js', new Date());

      gtag('config', 'G-4GRJLSSNZQ');
    </script>
</head>
<body>
<div class="w-full">
    <div class="w-full h-2 bg-blue-light"></div>
    <div class="w-full h-2 bg-pink-light"></div>
</div>
<main class="max-w-screen-md mx-auto py-16 px-6">
    <div class="flex justify-between items-center md:flex-row flex-col-reverse">
        <a href="/" class="prose max-w-none py-16 flex items-center justify-between cursor-pointer">
            <h1 class="main-headline">Trans<br/>Hilfe<br/>Elmshorn</h1>
        </a>
        <img class="h-48 md:mr-20 mx-auto" src="/logo.jpg"/>
    </div>
    <Navigation/>
    <slot/>
</main>
<div class="w-full">
</div>
<footer class="relative w-full flex justify-center bg-white text-xs text-slate-500">
    <span class="py-2">Made with 🏳️‍⚧️🤍 in Elmshorn · {currentYear}</span>
</footer>
</body>
</html>

<style is:global>
    html {
        font-family: system-ui, sans-serif;
    }

    .main-headline {
        @apply text-6xl text-center md:text-left;
        display: inline-block;
        background: -webkit-linear-gradient(120deg, #5BCEFA 0%, #F5A9B8 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }
</style>
